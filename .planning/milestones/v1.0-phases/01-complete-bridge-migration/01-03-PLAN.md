---
phase: 01-complete-bridge-migration
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - package.json
  - scripts/monitor/verify.mjs
  - README.md
autonomous: true
requirements:
  - MIGR-01
  - MIGR-02
user_setup: []
must_haves:
  truths:
    - "User can run one scripted verification command that proves the browser-first migration path still works."
    - "The verification flow checks dashboard reachability, API reachability, and a one-shot bridge publish path."
    - "The README explains how to run the smoke verification and what prerequisites it needs."
  artifacts:
    - path: "scripts/monitor/verify.mjs"
      provides: "Phase-specific smoke verifier for bridge/API/dashboard flow"
      min_lines: 60
      contains: "--once"
    - path: "package.json"
      provides: "Root verification command"
      contains: "\"monitor:verify\""
    - path: "README.md"
      provides: "Verification instructions"
      contains: "monitor:verify"
  key_links:
    - from: "scripts/monitor/verify.mjs"
      to: "scripts/monitor/shared.mjs"
      via: "shared constants and readiness helpers"
      pattern: "shared"
    - from: "scripts/monitor/verify.mjs"
      to: "http://127.0.0.1:8000"
      via: "API readiness and data checks"
      pattern: "8000"
    - from: "scripts/monitor/verify.mjs"
      to: "http://127.0.0.1:8540"
      via: "dashboard readiness checks"
      pattern: "8540"
---

<objective>
Add the repeatable smoke verification story for the migration.

Purpose: Make the new bridge path provable and repeatable instead of relying on scattered manual commands.
Output: `monitor:verify` script, package wiring, and smoke-test instructions.
</objective>

<execution_context>
@C:/Users/Lawrence/Documents/Dev/bluetti/bluetti-monitor/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/Documents/Dev/bluetti/bluetti-monitor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-complete-bridge-migration/01-CONTEXT.md
@.planning/phases/01-complete-bridge-migration/01-RESEARCH.md
@.planning/phases/01-complete-bridge-migration/01-VALIDATION.md
@.planning/phases/01-complete-bridge-migration/01-01-PLAN.md
@.planning/phases/01-complete-bridge-migration/01-02-PLAN.md
@package.json
@README.md
@docker-compose.yml
@lib/bluetti-mqtt-node/README.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement the scripted smoke verifier</name>
  <files>scripts/monitor/verify.mjs</files>
  <read_first>scripts/monitor/shared.mjs, package.json, docker-compose.yml, README.md, lib/bluetti-mqtt-node/README.md</read_first>
  <action>Create `scripts/monitor/verify.mjs` using the shared helpers from `scripts/monitor/shared.mjs`. The script must verify the canonical migration path with concrete checks: ensure `docker compose up -d` services are reachable at `http://127.0.0.1:8000` and `http://127.0.0.1:8540`, resolve the target device with discovery-plus-fallback behavior, run the package CLI with `--broker mqtt://127.0.0.1:1883 --once <MAC>`, then poll the API until `/devices` or `/status` shows device data. Exit non-zero with a clear failure reason if any step fails.</action>
  <verify>node --check scripts/monitor/verify.mjs</verify>
  <acceptance_criteria>
    - scripts/monitor/verify.mjs contains `http://127.0.0.1:8000`
    - scripts/monitor/verify.mjs contains `http://127.0.0.1:8540`
    - scripts/monitor/verify.mjs contains `--once`
    - scripts/monitor/verify.mjs contains `bluetti-mqtt-node`
  </acceptance_criteria>
  <done>The repo has one script that performs a bridge/API/dashboard smoke check for the migrated path.</done>
</task>

<task type="auto">
  <name>Task 2: Register and document the verification command</name>
  <files>package.json, README.md</files>
  <read_first>package.json, README.md, scripts/monitor/verify.mjs, .planning/phases/01-complete-bridge-migration/01-CONTEXT.md</read_first>
  <action>Add `monitor:verify` to `package.json` with the exact command `node ./scripts/monitor/verify.mjs`. Update `README.md` with a verification section that tells the user to run `npm run monitor:verify`, states the prerequisites explicitly as "Docker running, AC500 powered on, Bluetooth available on the Windows host," and explains that success means the dashboard and API are reachable and the one-shot bridge publish is visible through the API.</action>
  <verify>node -e "const pkg=require('./package.json'); if(pkg.scripts['monitor:verify']!=='node ./scripts/monitor/verify.mjs') process.exit(1)"</verify>
  <acceptance_criteria>
    - package.json contains `"monitor:verify": "node ./scripts/monitor/verify.mjs"`
    - README.md contains `npm run monitor:verify`
    - README.md contains `Docker running, AC500 powered on, Bluetooth available on the Windows host`
    - README.md contains language describing API and dashboard reachability as success conditions
  </acceptance_criteria>
  <done>The migration has a documented, repeatable verification command tied into the repo's command surface.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check scripts/monitor/verify.mjs` passes
- [ ] `node -e "const pkg=require('./package.json'); console.log(pkg.scripts['monitor:verify'])"` prints `node ./scripts/monitor/verify.mjs`
- [ ] `npm run monitor:verify` succeeds in a real Windows/BLE environment with the device available
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The migration path can be re-validated with one documented repo command
</success_criteria>

<output>
After completion, create `.planning/phases/01-complete-bridge-migration/01-03-SUMMARY.md`
</output>
