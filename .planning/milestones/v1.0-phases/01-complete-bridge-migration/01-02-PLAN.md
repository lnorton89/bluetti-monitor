---
phase: 01-complete-bridge-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - README.md
  - src/bun/index.ts
autonomous: true
requirements:
  - MIGR-02
user_setup: []
must_haves:
  truths:
    - "User-facing docs describe `npm run monitor:start` as the canonical setup and startup flow."
    - "The desktop wrapper is documented as optional convenience tooling rather than the primary runtime path."
    - "The non-local desktop bootstrap no longer waits on port 5173 when the packaged dashboard lives on port 8540."
  artifacts:
    - path: "README.md"
      provides: "Single consistent setup and startup story"
      min_lines: 150
      contains: "monitor:start"
    - path: "src/bun/index.ts"
      provides: "Desktop bootstrap aligned to the supported dashboard URL split"
      min_lines: 200
      contains: "8540"
  key_links:
    - from: "README.md"
      to: "package.json"
      via: "documented root startup command"
      pattern: "monitor:start"
    - from: "src/bun/index.ts"
      to: "docker-compose.yml"
      via: "packaged dashboard URL alignment"
      pattern: "8540"
---

<objective>
Align docs and desktop bootstrap language around the new bridge story.

Purpose: Remove the conflicting host-poller guidance and make the optional desktop wrapper stop implying it is the primary app path.
Output: Updated README guidance and corrected desktop bootstrap assumptions.
</objective>

<execution_context>
@C:/Users/Lawrence/Documents/Dev/bluetti/bluetti-monitor/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/Documents/Dev/bluetti/bluetti-monitor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-complete-bridge-migration/01-CONTEXT.md
@.planning/phases/01-complete-bridge-migration/01-RESEARCH.md
@.planning/phases/01-complete-bridge-migration/01-01-PLAN.md
@README.md
@package.json
@src/bun/index.ts
@docker-compose.yml
</context>

<tasks>
<task type="auto">
  <name>Task 1: Rewrite README around the canonical browser-first flow</name>
  <files>README.md</files>
  <read_first>README.md, package.json, docker-compose.yml, .planning/phases/01-complete-bridge-migration/01-CONTEXT.md</read_first>
  <action>Update `README.md` so the supported startup path is `npm run monitor:start`. Replace the current setup/start/open-dashboard flow with one canonical browser-first section that says the command starts Docker-backed services and the host bridge together. Keep desktop commands in a clearly separate "Optional desktop shell for local development" section. Remove remaining user-facing references to the old manual `bluetti-mqtt --broker localhost ...` host poller command. Keep `http://localhost:8540` as the primary dashboard URL and keep `5173` explicitly scoped to local dashboard development only.</action>
  <verify>Select-String -Path README.md -Pattern "monitor:start|8540|Optional desktop shell" | ForEach-Object { $_.Line }</verify>
  <acceptance_criteria>
    - README.md contains `npm run monitor:start`
    - README.md contains `http://localhost:8540`
    - README.md contains `Optional desktop shell`
    - README.md does not contain `bluetti-mqtt --broker localhost`
  </acceptance_criteria>
  <done>The README tells one consistent browser-first startup story and demotes the desktop wrapper to optional tooling.</done>
</task>

<task type="auto">
  <name>Task 2: Correct desktop bootstrap assumptions</name>
  <files>src/bun/index.ts</files>
  <read_first>src/bun/index.ts, docker-compose.yml, README.md</read_first>
  <action>Update `src/bun/index.ts` so the dashboard URL logic reflects the real split between local Vite development and packaged/containerized use. Keep `http://127.0.0.1:5173` only for the local-dev branch that starts Vite directly. In the non-local path, wait on and load `http://127.0.0.1:8540`. Also update any bootstrap error copy or status messaging that still implies the desktop wrapper is the primary supported path instead of an optional shell.</action>
  <verify>Select-String -Path src/bun/index.ts -Pattern "5173|8540|desktop shell|local dev stack" | ForEach-Object { $_.Line }</verify>
  <acceptance_criteria>
    - src/bun/index.ts contains `8540`
    - src/bun/index.ts keeps `5173` only for the local-dev Vite path
    - src/bun/index.ts no longer waits on `DASHBOARD_URL` pointing to `5173` in the non-local branch
  </acceptance_criteria>
  <done>The desktop wrapper no longer conflicts with the packaged/browser-first runtime story.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `Select-String -Path README.md -Pattern "monitor:start|8540"` returns the new canonical startup guidance
- [ ] `Select-String -Path README.md -Pattern "bluetti-mqtt --broker localhost"` returns nothing
- [ ] `Select-String -Path src/bun/index.ts -Pattern "8540"` returns the packaged dashboard URL handling
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Docs, desktop bootstrap, and runtime URL assumptions no longer tell conflicting startup stories
</success_criteria>

<output>
After completion, create `.planning/phases/01-complete-bridge-migration/01-02-SUMMARY.md`
</output>
