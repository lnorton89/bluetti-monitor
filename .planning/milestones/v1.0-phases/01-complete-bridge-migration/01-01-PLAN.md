---
phase: 01-complete-bridge-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/monitor/shared.mjs
  - scripts/monitor/start.mjs
autonomous: true
requirements:
  - MIGR-01
user_setup: []
must_haves:
  truths:
    - "User can run one root npm command to launch the supported browser-first monitoring flow without using the desktop wrapper."
    - "The root startup command launches bluetti-mqtt-node through the package CLI contract rather than through direct TypeScript imports."
  artifacts:
    - path: "scripts/monitor/shared.mjs"
      provides: "Shared runtime constants, LAN URL helpers, and process/readiness utilities"
      min_lines: 40
      contains: "24:4C:AB:2C:24:8E"
    - path: "scripts/monitor/start.mjs"
      provides: "Canonical browser-first monitor startup entrypoint"
      min_lines: 60
      contains: "docker compose up -d"
    - path: "package.json"
      provides: "Root startup command"
      contains: "\"monitor:start\""
  key_links:
    - from: "scripts/monitor/start.mjs"
      to: "docker-compose.yml"
      via: "spawned docker compose startup"
      pattern: "docker\\s+compose\\s+up\\s+-d"
    - from: "scripts/monitor/start.mjs"
      to: "node_modules/.bin/bluetti-mqtt-node.exe"
      via: "spawned package CLI"
      pattern: "bluetti-mqtt-node"
---

<objective>
Create the canonical browser-first startup command for Bluetti Monitor.

Purpose: Replace the split Docker-plus-manual-poller story with one repo-level command that launches the supported runtime path.
Output: Shared monitor orchestration helpers, a `monitor:start` entrypoint, and root script wiring.
</objective>

<execution_context>
@C:/Users/Lawrence/Documents/Dev/bluetti/bluetti-monitor/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Lawrence/Documents/Dev/bluetti/bluetti-monitor/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-complete-bridge-migration/01-CONTEXT.md
@.planning/phases/01-complete-bridge-migration/01-RESEARCH.md
@package.json
@docker-compose.yml
@README.md
@src/bun/index.ts
@src/bun/bluetooth.ts
@lib/bluetti-mqtt-node/README.md
@lib/bluetti-mqtt-node/package.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add shared browser-first runtime helpers</name>
  <files>scripts/monitor/shared.mjs</files>
  <read_first>package.json, docker-compose.yml, src/bun/index.ts, src/bun/bluetooth.ts, lib/bluetti-mqtt-node/README.md</read_first>
  <action>Create `scripts/monitor/shared.mjs` as the source of truth for the canonical startup constants and helpers. Define `DEFAULT_FALLBACK_MAC = "24:4C:AB:2C:24:8E"`, `BROKER_URL = "mqtt://127.0.0.1:1883"`, `API_URL = "http://127.0.0.1:8000"`, and `DASHBOARD_URL = "http://127.0.0.1:8540"`. Add reusable helpers for process spawning, shutdown cleanup, fetch-based readiness polling, LAN URL discovery from `os.networkInterfaces()`, and device resolution that tries discovery first and falls back to the known MAC if discovery returns nothing.</action>
  <verify>node --check scripts/monitor/shared.mjs</verify>
  <acceptance_criteria>
    - scripts/monitor/shared.mjs contains `DEFAULT_FALLBACK_MAC = "24:4C:AB:2C:24:8E"`
    - scripts/monitor/shared.mjs contains `DASHBOARD_URL = "http://127.0.0.1:8540"`
    - scripts/monitor/shared.mjs contains helpers for LAN URL discovery and readiness polling
  </acceptance_criteria>
  <done>Shared monitor helpers exist and expose the canonical runtime constants and helper functions.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the canonical monitor:start entrypoint</name>
  <files>scripts/monitor/start.mjs</files>
  <read_first>scripts/monitor/shared.mjs, docker-compose.yml, README.md, lib/bluetti-mqtt-node/README.md, lib/bluetti-mqtt-node/package.json</read_first>
  <action>Create `scripts/monitor/start.mjs` to run `docker compose up -d`, wait for `http://127.0.0.1:8000` and `http://127.0.0.1:8540`, resolve the target device with discovery-plus-fallback behavior from `shared.mjs`, then spawn the local package CLI from the repo root using the linked executable in `node_modules/.bin` on Windows. Pass the bridge concrete values that match the repo stack: `--broker mqtt://127.0.0.1:1883` and the resolved MAC address. Keep the process attached so the bridge keeps running, and print both the local dashboard URL and any LAN URLs without attempting to open a browser window.</action>
  <verify>node --check scripts/monitor/start.mjs</verify>
  <acceptance_criteria>
    - scripts/monitor/start.mjs contains `docker compose up -d`
    - scripts/monitor/start.mjs contains `http://127.0.0.1:8540`
    - scripts/monitor/start.mjs contains `bluetti-mqtt-node`
    - scripts/monitor/start.mjs does not contain browser-open logic such as `start ` or `explorer `
  </acceptance_criteria>
  <done>The start script launches the Docker-backed app stack and the package CLI bridge as one browser-first command.</done>
</task>

<task type="auto">
  <name>Task 3: Register the root startup command</name>
  <files>package.json</files>
  <read_first>package.json, scripts/monitor/start.mjs</read_first>
  <action>Add a root npm script named `monitor:start` with the exact command `node ./scripts/monitor/start.mjs`. Keep the existing desktop scripts intact, but do not make them aliases of the canonical startup command in this plan.</action>
  <verify>node -e "const pkg=require('./package.json'); if(pkg.scripts['monitor:start']!=='node ./scripts/monitor/start.mjs') process.exit(1)"</verify>
  <acceptance_criteria>
    - package.json contains `"monitor:start": "node ./scripts/monitor/start.mjs"`
    - package.json still contains `desktop:start`
    - package.json still contains `desktop:dev`
  </acceptance_criteria>
  <done>The repo has a single explicit root startup command for the supported migration path.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `node --check scripts/monitor/shared.mjs` passes
- [ ] `node --check scripts/monitor/start.mjs` passes
- [ ] `node -e "const pkg=require('./package.json'); console.log(pkg.scripts['monitor:start'])"` prints `node ./scripts/monitor/start.mjs`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The supported app startup path now exists as one root npm command
</success_criteria>

<output>
After completion, create `.planning/phases/01-complete-bridge-migration/01-01-SUMMARY.md`
</output>
