---
phase: 13
plan: 01
title: Add settings route, navigation entry, and preference store foundation
type: execute
wave: 1
files_modified:
  - dashboard/src/App.tsx
  - dashboard/src/components/Sidebar.tsx
  - dashboard/src/lib/routes.ts
  - dashboard/src/pages/Settings.tsx
  - dashboard/src/store
  - dashboard/src/index.css
requirements_addressed:
  - SET-01
  - SET-04
  - SET-06
  - SET-09
must_haves:
  - A dedicated settings page route exists in the dashboard shell
  - Settings have a typed/defaulted persistence model with safe fallback behavior
  - The first version makes it explicit which options are app preferences rather than live device controls
autonomous: true
---

<context>
The app already exposes some preference-like behavior, such as theme toggling and notification status, but those controls are scattered. A settings page should centralize supported app preferences without implying unsupported AC500 hardware control.
</context>

<objective>
Lay the groundwork for a proper settings page by adding the route/navigation shell, a preference storage model, and a first-pass settings surface that makes ownership and persistence boundaries explicit.
</objective>

<tasks>

<task id="1">
<name>Define settings route and page scaffold</name>
<type>execute</type>
<files>
  - dashboard/src/App.tsx
  - dashboard/src/lib/routes.ts
  - dashboard/src/components/Sidebar.tsx
  - dashboard/src/pages/Settings.tsx
</files>
<read_first>
  - dashboard/src/App.tsx
  - dashboard/src/lib/routes.ts
  - dashboard/src/components/Sidebar.tsx
  - dashboard/src/pages/Overview.tsx
  - dashboard/src/components/PageHeader.tsx
</read_first>
<action>
Add a dedicated Settings page route to the dashboard shell and make it discoverable from the existing navigation model.

Concrete expectations:
1. Add a new route definition in the same style as Overview/Charts/Solar/Raw Data.
2. Add a `dashboard/src/pages/Settings.tsx` page using the current shared page primitives.
3. Add the Settings entry to the sidebar/navigation metadata with copy that clearly frames it as app preferences.
4. Ensure the shell title/route signal stays coherent when the user enters the settings page.
</action>
<done>
  - `dashboard/src/pages/Settings.tsx` exists
  - `dashboard/src/App.tsx` includes the new route
  - `dashboard/src/lib/routes.ts` includes Settings metadata
  - `dashboard/src/components/Sidebar.tsx` renders Settings in navigation
</done>
<verify>
Run `bun run build` in `dashboard/`.
</verify>
</task>

<task id="2">
<name>Create a typed persisted preference model with safe fallback behavior</name>
<type>execute</type>
<files>
  - dashboard/src/store
  - dashboard/src/pages/Settings.tsx
</files>
<read_first>
  - dashboard/src/store/theme.ts
  - dashboard/src/store/shell.ts
  - dashboard/src/pages/Settings.tsx
</read_first>
<action>
Create the first settings-state model for app-level preferences, using the project’s existing store patterns and persistence approach.

Concrete expectations:
1. Define a typed preference shape with explicit defaults.
2. Persist settings across reloads using the project’s current frontend state approach.
3. Handle missing/invalid persisted data safely by falling back to defaults instead of breaking the page.
4. Keep the scope to app-owned settings only; do not blur into unsupported device-control state.
</action>
<done>
  - A dedicated settings store or equivalent preference model exists
  - Defaults are explicit and used when persisted values are missing or invalid
  - The settings page reads from the same source of truth the rest of the app will use
</done>
<verify>
Run `bun run build` in `dashboard/`.
</verify>
</task>

<task id="3">
<name>Make ownership boundaries explicit in the first-pass settings UI</name>
<type>execute</type>
<files>
  - dashboard/src/pages/Settings.tsx
  - dashboard/src/index.css
</files>
<read_first>
  - dashboard/src/pages/Settings.tsx
  - dashboard/src/index.css
  - dashboard/src/components/SectionPanel.tsx
  - dashboard/src/components/InfoRow.tsx
</read_first>
<action>
Design the initial settings page so it clearly communicates what the app can configure now versus what is only observed from live telemetry.

Concrete expectations:
1. Group the first-pass settings into intentional sections such as Appearance, Alerts, and Dashboard Behavior.
2. Add concise copy that prevents users from confusing app preferences with AC500 device control.
3. Leave room for future settings growth without shipping a sparse, fake control panel.
4. Keep the page visually aligned with the v1.1 shared UI system and mobile reliability patterns.
</action>
<done>
  - Settings UI uses grouped sections rather than a flat list
  - The page includes clear explanatory copy about app preferences vs device telemetry
  - Styling matches existing shared dashboard surfaces
</done>
<verify>
Run `bun run build` in `dashboard/`.
</verify>
</task>

</tasks>

<verification>
1. Run `bun run build` in `dashboard/`
</verification>
