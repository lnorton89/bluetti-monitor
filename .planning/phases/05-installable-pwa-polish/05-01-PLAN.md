---
phase: "05"
plan: "05-01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - "dashboard/vite.config.ts"
  - "dashboard/index.html"
  - "dashboard/public/icons"
requirements_addressed:
  - "PWA-02"
autonomous: true
---

<objective>

Add manifest, icons, and installability support for the dashboard using vite-plugin-pwa.

</objective>

<read_first>

- `dashboard/vite.config.ts` — Current Vite configuration
- `dashboard/index.html` — Entry point for meta tags
- `dashboard/dist/favicon.svg` — Source for app icons

</read_first>

<tasks>

<task id="1">
<type>execute</type>
<files>
  - "dashboard/vite.config.ts"
</files>
<action>

Install vite-plugin-pwa:

```bash
npm install vite-plugin-pwa -D --prefix dashboard
```

Add PWA plugin to vite.config.ts:

```typescript
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    react(),
    VitePWA({
      registerType: 'prompt',
      includeAssets: ['favicon.svg'],
      manifest: {
        name: 'Bluetti Monitor',
        short_name: 'Bluetti',
        description: 'Personal monitoring dashboard for Bluetti AC500',
        theme_color: '#1a1a2e',
        background_color: '#0f0f1a',
        display: 'standalone',
        start_url: '/',
        icons: [
          {
            src: 'pwa-192x192.png',
            sizes: '192x192',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png'
          },
          {
            src: 'pwa-512x512.png',
            sizes: '512x512',
            type: 'image/png',
            purpose: 'any maskable'
          }
        ]
      },
      workbox: {
        globPatterns: ['**/*.{js,css,html,svg,png,woff2}'],
        runtimeCaching: [
          {
            urlPattern: /^https:\/\/fonts\.googleapis\.com\/.*/i,
            handler: 'CacheFirst',
            options: {
              cacheName: 'google-fonts-cache',
              expiration: {
                maxEntries: 10,
                maxAgeSeconds: 60 * 60 * 24 * 365
              }
            }
          }
        ]
      }
    })
  ],
  // ... existing config
})
```

</action>
<acceptance_criteria>

- vite-plugin-pwa installed
- PWA manifest auto-generated on build
- Service worker registered on page load

</acceptance_criteria>
<verify>

```bash
cd dashboard && npm run build
# Check dist/ for manifest.webmanifest and sw.js
```

</verify>
</task>

<task id="2">
<type>execute</type>
<files>
  - "dashboard/index.html"
</files>
<action>

Update index.html to include PWA meta tags for better mobile install prompts:

```html
<meta name="theme-color" content="#1a1a2e">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="Bluetti">
<link rel="apple-touch-icon" href="/pwa-192x192.png">
```

</action>
<acceptance_criteria>

- Theme color meta tag present
- Apple web app meta tags present for iOS installability

</acceptance_criteria>
</task>

<task id="3">
<type>execute</type>
<files>
  - "dashboard/public"
</files>
<action>

Generate or copy PWA icon files. Create the public directory if needed and add placeholder icons. The build process will handle them if using vite-plugin-pwa's autoGenerate option, or create manually:

For now, add a note that icons can be generated from the existing favicon.svg using a tool like realfavicongenerator.net.

</action>
<acceptance_criteria>

- PWA icons available (can be placeholder for now)
- 192x192 and 512x512 icons present

</acceptance_criteria>
</task>

</tasks>

<verification>

1. Build the dashboard: `npm run build --prefix dashboard`
2. Check dist/ for generated files:
   - `manifest.webmanifest`
   - `sw.js` (service worker)
   - `workbox-*.js`
3. Serve dist/ locally and check browser DevTools Application tab for PWA installability

</verification>

<success_criteria>

- PWA manifest is valid and complete
- Service worker caches static assets
- Dashboard is installable on mobile browsers
- No errors in browser console related to PWA

</success_criteria>
