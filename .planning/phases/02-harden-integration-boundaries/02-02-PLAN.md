# Plan: 02-02 Fix bridge startup, polling, and runtime bugs needed for daily use

**Phase:** 02-harden-integration-boundaries
**Goal:** Fix bridge-related bugs that affect daily reliability
**Requirements:** [INTG-01]

---

## Analyze current bridge behavior

### Read the bridge launcher code

```bash
# Find where the bridge is launched from desktop shell
cat src/bun/bluetooth.ts
```

### Read the bridge CLI entrypoint

```bash
cat lib/bluetti-mqtt-node/src/cli/bluetooth-mqtt.ts
```

### Identify known issues

Check STATE.md and any recent commits for reported bridge issues:

```bash
# Check recent bridge-related changes
git log --oneline -20

# Check for any open issues or bugs
grep -r "bug\|fix\|error\|fail" --include="*.md" .planning/
```

---

## Test bridge startup

### Run monitor:start and observe

```bash
npm run monitor:start
```

[Note: This will start Docker services. Monitor the output for any errors.]

### Check for common issues

- [ ] Bridge discovers AC500 device successfully
- [ ] Bridge connects to MQTT broker at localhost
- [ ] Bridge starts polling and publishes telemetry
- [ ] No unhandled exceptions in the output

### Fix identified issues

For each bug identified:

1. **Document the bug** in this plan
2. **Identify the fix** approach
3. **Implement the fix** in the appropriate file
4. **Test the fix** by re-running monitor:start

---

## Known areas to check

Based on prior phase context, common bridge issues include:

- **Device discovery**: Does `discoverAC500()` work correctly?
- **MQTT connection**: Does the bridge reconnect on broker restart?
- **Polling intervals**: Is the polling frequency appropriate?
- **Error handling**: Are errors handled gracefully?

### Complete when

- [ ] Bridge starts successfully with `npm run monitor:start`
- [ ] Live telemetry appears in the dashboard
- [ ] No runtime errors during 5+ minutes of operation

---

## Verify INTG-01 completion

This plan addresses INTG-01: "User gets reliable live AC500 monitoring from the `bluetti-mqtt-node` bridge during normal startup and steady-state polling"

### Evidence checklist

- [ ] Bridge starts without errors
- [ ] Telemetry flows to MQTT broker
- [ ] Telemetry appears in API /status endpoint
- [ ] WebSocket receives updates

**If all evidence is ✓:** INTG-01 is SATISFIED.

---