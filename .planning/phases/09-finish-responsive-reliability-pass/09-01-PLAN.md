---
phase: 09
plan: 01
title: Stabilize the shared responsive shell and narrow-screen layout contract
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/App.tsx
  - dashboard/src/index.css
requirements_addressed:
  - UI-10
  - UI-11
  - UI-12
must_haves:
  - Phone-sized shell keeps one clear route identity plus one route signal without awkward wrapping or clipped status surfaces
  - Shared spacing, chip, grid, and card rules provide a stable mobile baseline before route-specific responsive work begins
  - Existing Phase 6 and Phase 8 shell/surface contracts remain intact
autonomous: true
---

<context>
Phase 09 should begin by tightening the shared shell and responsive CSS contract before touching route-specific screens. The current mobile behavior mostly comes from broad media-query collapse rules in `dashboard/src/index.css`, so wave 1 should define the narrow-screen baseline that every route can rely on.
</context>

<objective>
Make the shell, content gutters, shared tiles, chips, and card surfaces feel deliberate on phone-sized screens without changing route ownership or introducing a separate mobile navigation model.
</objective>

<tasks>

<task id="1">
<name>Refine the shell top-bar contract for phone-sized screens</name>
<type>execute</type>
<files>
  - dashboard/src/App.tsx
  - dashboard/src/index.css
</files>
<read_first>
  - dashboard/src/App.tsx
  - dashboard/src/index.css
  - dashboard/src/lib/routes.ts
  - .planning/phases/06-unify-shell-and-navigation/06-CONTEXT.md
  - .planning/phases/09-finish-responsive-reliability-pass/09-CONTEXT.md
</read_first>
<action>
Update the shell markup and the shared top-bar CSS so the narrow-screen shell stays readable and consistent with the Phase 6 thin-shell contract. Keep the existing route title and `data-testid="shell-route-signal"` surface, but make these concrete adjustments:

1. Keep the top bar in `dashboard/src/App.tsx` limited to:
   - the mobile drawer trigger
   - route identity copy
   - one route signal surface
   - no reintroduction of the desktop status cluster on phone widths
2. In `dashboard/src/index.css`, refine the `@media (max-width: 1024px)` and `@media (max-width: 480px)` rules so:
   - `.top-bar` uses stable vertical spacing and does not leave the route signal visually stranded
   - `.top-bar-copy`, `.top-bar-title`, and `.top-bar-signal` wrap cleanly on narrow screens
   - `.top-bar-signal` can hold both the label and live value without clipped text
   - `.hamburger-btn`, `.sidebar-nav-link`, and `.top-bar-metric` keep tap-safe sizing of at least the current 44px treatment
3. Preserve the existing route signal behavior and test selectors:
   - `data-testid="shell-title"`
   - `data-testid="shell-route-signal"`
4. Do not add a second mobile header or route hero. The route pages still own their contextual framing.
</action>
<done>
  - `dashboard/src/App.tsx` still contains `data-testid="shell-title"`
  - `dashboard/src/App.tsx` still contains `data-testid="shell-route-signal"`
  - `dashboard/src/index.css` contains `.top-bar-signal`
  - `dashboard/src/index.css` contains `@media (max-width: 1024px)`
  - `dashboard/src/index.css` contains `@media (max-width: 480px)`
</done>
<verify>
Run `npm --prefix dashboard run build`.
</verify>
</task>

<task id="2">
<name>Stabilize shared narrow-screen layout tokens before route-specific refactors</name>
<type>execute</type>
<files>
  - dashboard/src/index.css
</files>
<read_first>
  - dashboard/src/index.css
  - dashboard/src/components/ui.tsx
  - dashboard/src/components/PageHeader.tsx
  - dashboard/src/components/SectionPanel.tsx
  - dashboard/src/components/MetricTile.tsx
  - .planning/phases/08-consolidate-shared-ui-surfaces/08-VALIDATION.md
</read_first>
<action>
Tighten the shared responsive CSS in `dashboard/src/index.css` so route pages inherit a better mobile baseline before their page-specific work begins. Keep the existing shared-surface system, but add or refine narrow-screen behavior for these exact shared classes:

- `.app-content` and `.app-content-inner` for smaller gutters that still preserve breathing room
- `.card`, `.surface-card`, and `.section-header` so cards and headers keep readable spacing on phones
- `.tile-grid`, `.tile-grid--fit`, and `.tile-grid--detail` so shared metric grids collapse predictably
- `.chip`, `.chip--active`, `.device-pill-row`, `.analytics-segmented`, and `.ui-pill-button` so chips and segmented controls wrap cleanly and stay tap-safe
- `.info-row`, `.info-row-label`, and `.info-row-value` so stacked label/value rows still read as one unit on phones

Use route-agnostic token rules where possible, and leave route-specific sequencing for later plans. Do not remove the existing desktop behavior.
</action>
<done>
  - `dashboard/src/index.css` contains `.tile-grid`
  - `dashboard/src/index.css` contains `.chip`
  - `dashboard/src/index.css` contains `.info-row`
  - `dashboard/src/index.css` contains `.analytics-segmented`
  - `dashboard/src/index.css` contains `.device-pill-row`
</done>
<verify>
Run `npm --prefix dashboard run build`.
</verify>
</task>

</tasks>

<verification>
1. Run `npm --prefix dashboard run build`
</verification>

<must_haves>
1. Phone-sized shell keeps one clear route identity plus one route signal without awkward wrapping or clipped status surfaces
2. Shared spacing, chip, grid, and card rules provide a stable mobile baseline before route-specific responsive work begins
3. Existing Phase 6 and Phase 8 shell/surface contracts remain intact
</must_haves>
