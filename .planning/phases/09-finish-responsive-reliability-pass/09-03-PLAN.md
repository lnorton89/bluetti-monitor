---
phase: 09
plan: 03
title: Make Charts and Solar controls and report surfaces reliable on phones
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - dashboard/src/pages/Charts.tsx
  - dashboard/src/pages/Solar.tsx
  - dashboard/src/index.css
requirements_addressed:
  - UI-10
  - UI-11
  - UI-12
must_haves:
  - Charts segmented controls, scorecards, and report surfaces stay legible and tap-safe on phones
  - Solar segmented controls, scorecards, and mapping/detail surfaces stay legible and tap-safe on phones
  - Mobile sequencing across Charts and Solar still feels like the same shared dashboard product
autonomous: true
---

<context>
Charts and Solar already share most of the surface primitives introduced in Phase 8. Their remaining mobile risk is interaction density: multiple segmented controls, score grids, report meta chips, charts, and secondary details compete for space on narrow screens.
</context>

<objective>
Refine Charts and Solar so phone-sized layouts present controls, scorecards, charts, and secondary details in an intentional order without sacrificing the current analytics and solar information contract.
</objective>

<tasks>

<task id="1">
<name>Re-group Charts controls and report surfaces for small screens</name>
<type>execute</type>
<files>
  - dashboard/src/pages/Charts.tsx
  - dashboard/src/index.css
</files>
<read_first>
  - dashboard/src/pages/Charts.tsx
  - dashboard/src/index.css
  - dashboard/src/lib/shared-controls.ts
  - dashboard/src/components/PageHeader.tsx
  - dashboard/src/components/SectionPanel.tsx
  - dashboard/src/components/MetricTile.tsx
</read_first>
<action>
Update `dashboard/src/pages/Charts.tsx` and the matching analytics CSS blocks in `dashboard/src/index.css` so the page remains usable on phones. Preserve existing data logic, range presets, and route signals, but make these concrete adjustments:

1. Keep the mobile order predictable:
   - page header and status/meta chips
   - device selector
   - range selector
   - focus selector
   - scorecards
   - primary report chart
   - custom comparison and field explorer surfaces
2. Refine narrow-screen behavior for:
   - `.analytics-toolbar`
   - `.analytics-segmented`
   - `.analytics-focus-strip`
   - `.analytics-score-grid`
   - `.analytics-report-meta`
   - `.analytics-detail-grid`
   - `.analytics-field-row`
3. Ensure segmented control buttons stay tap-safe and wrap cleanly instead of shrinking into cramped inline rows.
4. Keep the custom comparison and field explorer usable on phones, even if secondary metadata stacks vertically.
5. Do not change the underlying analytics calculations, React Query usage, or route-signal contract.
</action>
<done>
  - `dashboard/src/pages/Charts.tsx` contains `analytics-segmented`
  - `dashboard/src/pages/Charts.tsx` contains `analytics-score-grid`
  - `dashboard/src/pages/Charts.tsx` contains `analytics-detail-grid`
  - `dashboard/src/index.css` contains `.analytics-toolbar`
  - `dashboard/src/index.css` contains `.analytics-field-row`
  - `dashboard/src/index.css` contains `.analytics-report-meta`
</done>
<verify>
Run `npm --prefix dashboard run build`.
</verify>
</task>

<task id="2">
<name>Re-group Solar controls and mapping/detail surfaces for small screens</name>
<type>execute</type>
<files>
  - dashboard/src/pages/Solar.tsx
  - dashboard/src/index.css
</files>
<read_first>
  - dashboard/src/pages/Solar.tsx
  - dashboard/src/index.css
  - dashboard/src/lib/shared-controls.ts
  - dashboard/src/components/InfoRow.tsx
  - dashboard/src/components/SectionPanel.tsx
  - dashboard/src/components/MetricTile.tsx
</read_first>
<action>
Update `dashboard/src/pages/Solar.tsx` and the matching solar CSS blocks in `dashboard/src/index.css` so the page remains usable on phones. Preserve existing field aliasing, report logic, and route signals, but make these exact adjustments:

1. Keep the mobile order predictable:
   - page header and meta chips
   - device selector
   - range selector
   - focus selector
   - scorecards
   - primary solar report
   - input-detail cards
   - forecast and mapping sections
2. Refine narrow-screen behavior for:
   - `.analytics-toolbar`
   - `.analytics-segmented`
   - `.solar-score-grid`
   - `.solar-detail-grid`
   - `.solar-string-grid`
   - `.solar-input-card-header`
   - `.solar-input-card-row`
   - `.solar-mapping-row`
3. Keep split electrical views readable on phones even when PV1 and PV2 cards stack vertically.
4. Keep the forecast, mapping rows, and supporting detail sections understandable without hiding mapped field names or summary values.
5. Do not change the underlying solar calculations, alias resolution, or route-signal contract.
</action>
<done>
  - `dashboard/src/pages/Solar.tsx` contains `analytics-segmented`
  - `dashboard/src/pages/Solar.tsx` contains `solar-score-grid`
  - `dashboard/src/pages/Solar.tsx` contains `solar-mapping-row`
  - `dashboard/src/index.css` contains `.solar-detail-grid`
  - `dashboard/src/index.css` contains `.solar-string-grid`
  - `dashboard/src/index.css` contains `.solar-input-card-row`
</done>
<verify>
Run `npm --prefix dashboard run build`.
</verify>
</task>

</tasks>

<verification>
1. Run `npm --prefix dashboard run build`
</verification>

<must_haves>
1. Charts segmented controls, scorecards, and report surfaces stay legible and tap-safe on phones
2. Solar segmented controls, scorecards, and mapping/detail surfaces stay legible and tap-safe on phones
3. Mobile sequencing across Charts and Solar still feels like the same shared dashboard product
</must_haves>
