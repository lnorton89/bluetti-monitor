---
phase: 09
plan: 02
title: Make Overview and Raw Data intentionally usable on phones
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - dashboard/src/pages/Overview.tsx
  - dashboard/src/pages/RawData.tsx
  - dashboard/src/index.css
requirements_addressed:
  - UI-10
  - UI-11
  - UI-12
must_haves:
  - Overview preserves clear hero, reserve, and detail hierarchy on phones
  - Raw Data keeps device switching, search, and field browsing intentionally usable on phones
  - Trust-state surfaces from Phase 7 remain visible and understandable after the responsive pass
autonomous: true
---

<context>
Overview and Raw Data are the two routes where mobile usability depends most on hierarchy rather than simple component reuse. Overview needs a stronger small-screen narrative, and Raw Data needs an explorer-first mobile contract rather than a desktop table simply squeezed smaller.
</context>

<objective>
Refine the Overview and Raw Data routes so they remain readable and explorable on phone-sized screens while preserving the same telemetry contract and trust-state behavior as desktop.
</objective>

<tasks>

<task id="1">
<name>Re-sequence Overview for narrow screens without losing telemetry hierarchy</name>
<type>execute</type>
<files>
  - dashboard/src/pages/Overview.tsx
  - dashboard/src/index.css
</files>
<read_first>
  - dashboard/src/pages/Overview.tsx
  - dashboard/src/index.css
  - dashboard/src/hooks/useTelemetryState.ts
  - dashboard/src/components/BatteryEstimates.tsx
  - .planning/phases/07-fix-telemetry-trust-states/07-CONTEXT.md
</read_first>
<action>
Update `dashboard/src/pages/Overview.tsx` and the matching overview CSS blocks in `dashboard/src/index.css` so the route reads intentionally on phones. Keep the same data and trust-state behavior, but make these concrete changes:

1. Preserve the existing route structure:
   - stale indicator
   - offline banner
   - device overview header
   - hero
   - summary tiles
   - detail panels
   - switchboard
2. Refine the narrow-screen layout so:
   - the hero battery summary and mode/status messaging remain visually first
   - summary metric tiles keep a readable order instead of flattening into a generic wall
   - detail panels stack in a predictable reading order with enough spacing between sections
   - power-flow split rows and hero sub-rows become stacked label/value groups when needed
3. Keep the Phase 7 trust-state surfaces visible and unambiguous:
   - `.stale-indicator`
   - `.offline-banner`
   - existing loading skeleton behavior
4. Do not hide core overview values such as reserve, live status, input/output, or key identity rows just to make the layout fit.
</action>
<done>
  - `dashboard/src/pages/Overview.tsx` contains `stale-indicator`
  - `dashboard/src/pages/Overview.tsx` contains `offline-banner`
  - `dashboard/src/pages/Overview.tsx` contains `MetricTile`
  - `dashboard/src/index.css` contains `.hero-grid`
  - `dashboard/src/index.css` contains `.summary-grid`
  - `dashboard/src/index.css` contains `.power-node-split`
</done>
<verify>
Run `npm --prefix dashboard run build`.
</verify>
</task>

<task id="2">
<name>Make Raw Data mobile exploration deliberate instead of incidental</name>
<type>execute</type>
<files>
  - dashboard/src/pages/RawData.tsx
  - dashboard/src/index.css
</files>
<read_first>
  - dashboard/src/pages/RawData.tsx
  - dashboard/src/index.css
  - dashboard/src/lib/fields.ts
  - dashboard/src/hooks/useTelemetryState.ts
  - dashboard/tests/dashboard.spec.ts
</read_first>
<action>
Refine `dashboard/src/pages/RawData.tsx` and the matching Raw Data CSS blocks in `dashboard/src/index.css` so the phone-sized view is clearly usable. Keep device switching, search, and the field inventory contract intact, but make these exact adjustments:

1. Keep the control order fixed on phones:
   - page header
   - device selector or active-device chip
   - search
   - field browser/table
2. Improve narrow-screen behavior for:
   - `.device-pill-row`
   - `.workspace-search`
   - `.data-table-shell`
   - `.data-table-scroll`
   - `.data-table`
3. Preserve the current searchable field browser behavior:
   - the search input still filters by raw field key and display label
   - multi-device switching still works when more than one device is present
4. If the desktop-style table remains too dense on phones, change the narrow-screen presentation to a stacked field-row layout or another explicit mobile explorer presentation, but preserve the same information fields:
   - field key
   - label
   - category
   - value
   - last updated
5. Keep the existing trust-state surfaces and empty-state behavior visible on mobile.
</action>
<done>
  - `dashboard/src/pages/RawData.tsx` contains `Search fields...`
  - `dashboard/src/pages/RawData.tsx` contains `device-pill-row`
  - `dashboard/src/pages/RawData.tsx` contains `data-table-shell`
  - `dashboard/src/index.css` contains `.data-table-shell`
  - `dashboard/src/index.css` contains `.data-table-scroll`
  - `dashboard/src/index.css` contains `.workspace-search`
</done>
<verify>
Run `npm --prefix dashboard run build`.
</verify>
</task>

</tasks>

<verification>
1. Run `npm --prefix dashboard run build`
</verification>

<must_haves>
1. Overview preserves clear hero, reserve, and detail hierarchy on phones
2. Raw Data keeps device switching, search, and field browsing intentionally usable on phones
3. Trust-state surfaces from Phase 7 remain visible and understandable after the responsive pass
</must_haves>
