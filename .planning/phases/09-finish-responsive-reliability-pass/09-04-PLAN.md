---
phase: 09
plan: 04
title: Add responsive regression coverage for the phone-sized dashboard contract
type: execute
wave: 4
depends_on:
  - 09-03
files_modified:
  - dashboard/tests/phase9-responsive.spec.ts
  - dashboard/tests/dashboard.spec.ts
  - dashboard/tests/phase8-shared-ui.spec.ts
  - dashboard/playwright.config.ts
requirements_addressed:
  - UI-10
  - UI-11
  - UI-12
must_haves:
  - Dedicated Playwright coverage exists for phone-sized responsive behavior across all main routes
  - Existing smoke and shared-surface tests remain aligned with the final responsive contract
  - The full repo-local Playwright suite passes after the responsive pass
autonomous: true
---

<context>
Responsive reliability is the actual deliverable for Phase 9, so the phase should end with dedicated phone-layout regression coverage rather than relying on build-only confidence or manual testing.
</context>

<objective>
Create focused Playwright coverage for phone-sized layouts and keep the existing smoke/shared-surface suites aligned with the final responsive contract.
</objective>

<tasks>

<task id="1">
<name>Add a dedicated phone-sized responsive regression spec</name>
<type>execute</type>
<files>
  - dashboard/tests/phase9-responsive.spec.ts
</files>
<read_first>
  - dashboard/tests/dashboard.spec.ts
  - dashboard/tests/phase8-shared-ui.spec.ts
  - dashboard/src/App.tsx
  - dashboard/src/pages/Overview.tsx
  - dashboard/src/pages/RawData.tsx
  - dashboard/src/pages/Charts.tsx
  - dashboard/src/pages/Solar.tsx
  - .planning/phases/09-finish-responsive-reliability-pass/09-VALIDATION.md
</read_first>
<action>
Create `dashboard/tests/phase9-responsive.spec.ts` as the dedicated responsive regression spec for the Phase 9 contract. Run it in mock mode at a phone-sized viewport such as `390x844` or `430x932`, and cover these exact behaviors:

1. Shell and navigation:
   - open the mobile drawer with the `Open Navigation` button
   - navigate across Overview, Charts, Solar, and Raw Data
   - verify `data-testid="shell-title"` and `data-testid="shell-route-signal"` keep updating correctly on mobile
2. Overview:
   - confirm the page still exposes the overview hero/device header and key reserve/battery content
   - confirm the summary/detail layout remains visible without a broken control or clipped card surface
3. Charts:
   - confirm the range segmented control remains usable
   - switch the focus control and verify the report surface updates without layout breakage
4. Solar:
   - confirm the range/focus controls remain usable
   - verify score/report or mapping content remains visible after the control interaction
5. Raw Data:
   - confirm the search input is visible and usable
   - if multiple devices are available in mock mode, switch devices and confirm the explorer still updates
   - confirm field browsing remains possible after filtering

Use stable labels, `data-testid` selectors, and existing visible text where available. Avoid screenshot-only assertions as the primary verification mechanism.
</action>
<done>
  - `dashboard/tests/phase9-responsive.spec.ts` exists
  - `dashboard/tests/phase9-responsive.spec.ts` contains `shell-title`
  - `dashboard/tests/phase9-responsive.spec.ts` contains `shell-route-signal`
  - `dashboard/tests/phase9-responsive.spec.ts` contains `Open Navigation`
  - `dashboard/tests/phase9-responsive.spec.ts` contains `Search fields...`
</done>
<verify>
Run `npm --prefix dashboard run test:e2e -- tests/phase9-responsive.spec.ts`.
</verify>
</task>

<task id="2">
<name>Keep the existing Playwright suite aligned with the final responsive contract</name>
<type>execute</type>
<files>
  - dashboard/tests/dashboard.spec.ts
  - dashboard/tests/phase8-shared-ui.spec.ts
  - dashboard/playwright.config.ts
</files>
<read_first>
  - dashboard/tests/dashboard.spec.ts
  - dashboard/tests/phase8-shared-ui.spec.ts
  - dashboard/playwright.config.ts
  - dashboard/tests/phase9-responsive.spec.ts
  - .planning/phases/08-consolidate-shared-ui-surfaces/08-VALIDATION.md
</read_first>
<action>
Review the existing Playwright suite after the responsive work lands and keep it aligned with the final contract:

1. Update `dashboard/tests/dashboard.spec.ts` only if the final mobile/smoke flow changed enough that the assertions became stale.
2. Update `dashboard/tests/phase8-shared-ui.spec.ts` only where the responsive pass changes the route sequencing or visible text used by the shared-surface checks.
3. Keep `dashboard/playwright.config.ts` aligned with the repo-local dev server and mock-mode flow. Preserve the existing base URL and local Vite command unless a concrete failure during the new responsive spec requires a targeted adjustment.
4. Finish by running the full repo-local Playwright suite so the responsive spec and the existing suites all pass together.
</action>
<done>
  - `dashboard/playwright.config.ts` contains `http://127.0.0.1:4173`
  - `dashboard/playwright.config.ts` contains `npm run dev -- --host 127.0.0.1 --port 4173`
  - `npm --prefix dashboard run test:e2e` exits 0
</done>
<verify>
Run `npm --prefix dashboard run test:e2e`.
</verify>
</task>

</tasks>

<verification>
1. Run `npm --prefix dashboard run test:e2e -- tests/phase9-responsive.spec.ts`
2. Run `npm --prefix dashboard run test:e2e`
</verification>

<must_haves>
1. Dedicated Playwright coverage exists for phone-sized responsive behavior across all main routes
2. Existing smoke and shared-surface tests remain aligned with the final responsive contract
3. The full repo-local Playwright suite passes after the responsive pass
</must_haves>
