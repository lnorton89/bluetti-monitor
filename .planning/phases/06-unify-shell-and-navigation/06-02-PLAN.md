---
phase: 06
plan: 02
title: Refactor the shell into a thin top bar with shared desktop and mobile navigation
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - dashboard/src/App.tsx
  - dashboard/src/components/Sidebar.tsx
  - dashboard/src/index.css
requirements_addressed:
  - UI-01
  - UI-02
  - UI-03
must_haves:
  - The persistent route hero is removed from the shell
  - The shell keeps only route identity plus operational status
  - Desktop sidebar and mobile drawer remain the same navigation model
autonomous: true
---

<context>
With metadata and selectors in place, the next step is the actual shell cleanup: remove the competing route hero, keep a thin top bar, and make the sidebar and drawer behave like the same navigation system across breakpoints.
</context>

<objective>
Replace the overloaded shell with a thin, registry-driven top bar and a consistent sidebar/drawer navigation frame.
</objective>

<tasks>

<task id="1">
<name>Remove the shell route hero and keep only operational shell status</name>
<type>execute</type>
<files>
  - dashboard/src/App.tsx
</files>
<read_first>
  - dashboard/src/App.tsx
  - dashboard/src/lib/routes.ts
  - dashboard/src/store/shell.ts
  - dashboard/src/lib/notifications.ts
  - dashboard/src/store/ws.ts
  - .planning/phases/06-unify-shell-and-navigation/06-UI-SPEC.md
</read_first>
<action>
Refactor `dashboard/src/App.tsx` so the persistent shell does all of the following:

- Remove the entire persistent `<section className="route-hero">...</section>` block
- Keep the sticky `.top-bar`
- Render exactly one route title surface using the route registry and `data-testid="shell-title"`
- Render exactly one mobile route signal surface using the shell store and `data-testid="shell-route-signal"`
- Keep desktop operational shell status limited to connection, freshness, battery percent, device count, and notification readiness
- Remove shell-level input, output, and net telemetry summaries entirely
- Preserve the browser-notification enable action when permission is still `default`

Do not introduce a second route headline, subtitle, or hero block in the shell.
</action>
<done>
  - `dashboard/src/App.tsx` does not contain `className="route-hero"`
  - `dashboard/src/App.tsx` contains `data-testid="shell-title"`
  - `dashboard/src/App.tsx` contains `data-testid="shell-route-signal"`
  - `dashboard/src/App.tsx` contains `shell-status-connection`
  - `dashboard/src/App.tsx` does not contain `<span>Input</span>`
  - `dashboard/src/App.tsx` does not contain `<span>Output</span>`
  - `dashboard/src/App.tsx` does not contain `<span>Net</span>`
</done>
<verify>
Run `npm --prefix dashboard run build`.
</verify>
</task>

<task id="2">
<name>Align sidebar and drawer behavior with the thin-shell contract</name>
<type>execute</type>
<files>
  - dashboard/src/components/Sidebar.tsx
  - dashboard/src/index.css
  - dashboard/src/App.tsx
</files>
<read_first>
  - dashboard/src/components/Sidebar.tsx
  - dashboard/src/index.css
  - dashboard/src/App.tsx
  - .planning/phases/06-unify-shell-and-navigation/06-UI-SPEC.md
</read_first>
<action>
Update the sidebar and shell styling so desktop and mobile use the same navigation model:

- Keep desktop sidebar width at `200px`
- Keep mobile drawer width at `min(280px, calc(100vw - 56px))`
- Keep `.top-bar` sticky
- Ensure the drawer closes on overlay click, close button click, and route selection
- Keep hamburger, drawer close button, nav rows, and the route-signal chip at or above `44px` touch height
- Hide the extra desktop chip cluster on mobile so the top bar shows only route title plus one route signal
- Remove the unused `.route-hero*` style rules from `dashboard/src/index.css`

Preserve the existing theme toggle and stack note inside the sidebar, but keep route navigation visually primary.
</action>
<done>
  - `dashboard/src/index.css` contains `width: 200px`
  - `dashboard/src/index.css` contains `min(280px, calc(100vw - 56px))`
  - `dashboard/src/index.css` does not contain `.route-hero`
  - `dashboard/src.index.css` is not a file reference in any import or code comment
  - `dashboard/src/components/Sidebar.tsx` contains `aria-label="Close navigation"`
  - `dashboard/src/App.tsx` contains `aria-label="Open Navigation"`
  - `dashboard/src/index.css` contains `min-height: 44px`
</done>
<verify>
Run `npm --prefix dashboard run build`.
</verify>
</task>

</tasks>

<verification>
1. Run `npm --prefix dashboard run build`
2. Read `dashboard/src/App.tsx` and confirm the persistent shell no longer renders a route hero
3. Read `dashboard/src/index.css` and confirm the top bar and drawer rules match the approved UI contract
</verification>

<must_haves>
1. The shell shows one route identity surface at a time
2. Desktop shell keeps only operational status
3. Desktop sidebar and mobile drawer remain the same navigation model
</must_haves>
