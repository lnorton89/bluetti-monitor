---
phase: 06
plan: 04
title: Rewrite Playwright coverage to match the final shell contract
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - dashboard/tests/dashboard.spec.ts
  - dashboard/tests/initial-layout.spec.ts
  - dashboard/playwright.config.ts
requirements_addressed:
  - UI-01
  - UI-02
  - UI-03
must_haves:
  - Quick Playwright coverage matches the final shell contract
  - Desktop layout coverage proves the route hero is gone
  - The full repo-local Playwright suite passes before phase verification
autonomous: true
---

<context>
With the final shell contract and route signals in place, the stale Playwright suite can now be rewritten against stable selectors instead of the old duplicated-title UI.
</context>

<objective>
Replace the stale shell assertions with Playwright coverage that matches the completed Phase 6 shell and navigation behavior.
</objective>

<tasks>

<task id="1">
<name>Rewrite the mobile drawer and route-signal Playwright flow</name>
<type>execute</type>
<files>
  - dashboard/tests/dashboard.spec.ts
</files>
<read_first>
  - dashboard/tests/dashboard.spec.ts
  - dashboard/src/App.tsx
  - dashboard/src/components/Sidebar.tsx
  - dashboard/src/pages/Charts.tsx
  - dashboard/src/pages/Solar.tsx
  - dashboard/src/pages/RawData.tsx
  - .planning/phases/06-unify-shell-and-navigation/06-VALIDATION.md
</read_first>
<action>
Rewrite `dashboard/tests/dashboard.spec.ts` so it validates the Phase 6 mobile shell behavior instead of the stale `Add Chart` assertion. Keep the mock-mode flow and cover these exact checks:

1. Open `/?mock=1` at a phone-sized viewport
2. Open the drawer with the `Open Navigation` button
3. Assert `data-testid="sidebar-nav"` is visible
4. Navigate to `Charts` and assert:
   - `data-testid="shell-title"` contains `Charts`
   - `data-testid="shell-route-signal"` contains `Window`
   - `data-testid="shell-route-signal"` also contains the active range text from `range.label`
   - the drawer closes after route selection
5. Navigate to `Solar` and assert:
   - `data-testid="shell-route-signal"` contains `Solar`
   - `data-testid="shell-route-signal"` contains a watt value formatted like `123 W` or the exact fallback format used by the page
6. Navigate to `Raw Data` and assert:
   - `data-testid="shell-route-signal"` contains `Fields`
   - `data-testid="shell-route-signal"` contains a value formatted like `12 visible`
7. Use the raw-data search input, search for `battery`, and assert `total_battery_percent` is visible
8. Re-open the drawer and verify the active route styling is attached to the Raw Data link

Keep one desktop-oriented sidebar assertion in the same file so the quick command still exercises both the shell and the main content surface. At desktop width, click a sidebar route such as `Solar`, then assert the shell title updates to `Solar` and the desktop sidebar link shows the active-route treatment.
</action>
<done>
  - `dashboard/tests/dashboard.spec.ts` does not contain `Add Chart`
  - `dashboard/tests/dashboard.spec.ts` contains `shell-title`
  - `dashboard/tests/dashboard.spec.ts` contains `shell-route-signal`
  - `dashboard/tests/dashboard.spec.ts` contains `Open Navigation`
  - `dashboard/tests/dashboard.spec.ts` contains `total_battery_percent`
  - `dashboard/tests/dashboard.spec.ts` contains `Solar`
</done>
<verify>
Run `npm --prefix dashboard run test:e2e -- tests/dashboard.spec.ts`.
</verify>
</task>

<task id="2">
<name>Rewrite the desktop shell-layout assertions</name>
<type>execute</type>
<files>
  - dashboard/tests/initial-layout.spec.ts
</files>
<read_first>
  - dashboard/tests/initial-layout.spec.ts
  - dashboard/src/App.tsx
  - dashboard/src/index.css
  - .planning/phases/06-unify-shell-and-navigation/06-RESEARCH.md
</read_first>
<action>
Rewrite `dashboard/tests/initial-layout.spec.ts` so it verifies the desktop shell hierarchy after the route hero removal. Keep the before/after resize screenshots, but replace the old overview-title assertion with these exact checks:

- `data-testid="shell-title"` is visible
- `.route-hero` count is `0`
- `.top-bar` is visible
- the Overview page content still renders, using a route-owned element such as `.device-header`

Do not reintroduce title-text assertions that depend on the old long-form shell copy.
</action>
<done>
  - `dashboard/tests/initial-layout.spec.ts` contains `shell-title`
  - `dashboard/tests/initial-layout.spec.ts` contains `.route-hero`
  - `dashboard/tests/initial-layout.spec.ts` contains `.device-header`
  - `dashboard/tests/initial-layout.spec.ts` does not contain `AC500 Power Station Monitor`
</done>
<verify>
Run `npm --prefix dashboard run test:e2e -- tests/initial-layout.spec.ts`.
</verify>
</task>

<task id="3">
<name>Keep Playwright config aligned and finish with a green full suite</name>
<type>execute</type>
<files>
  - dashboard/playwright.config.ts
</files>
<read_first>
  - dashboard/playwright.config.ts
  - dashboard/tests/dashboard.spec.ts
  - dashboard/tests/initial-layout.spec.ts
  - .planning/phases/06-unify-shell-and-navigation/06-VALIDATION.md
</read_first>
<action>
Review `dashboard/playwright.config.ts` after the test rewrite and keep it aligned with the Phase 6 validation contract. Preserve the current local web server command unless the refactored tests reveal a concrete need to adjust timeouts or baseURL behavior. If you do change the config, keep these exact values unless there is a demonstrated break:

- `baseURL: 'http://127.0.0.1:4173'`
- `command: 'npm run dev -- --host 127.0.0.1 --port 4173'`
- `url: 'http://127.0.0.1:4173/?mock=1'`

Finish by running the full Phase 6 suite so the quick test and the initial-layout test both pass under the repo-local Playwright package.
</action>
<done>
  - `dashboard/playwright.config.ts` contains `http://127.0.0.1:4173`
  - `dashboard/playwright.config.ts` contains `npm run dev -- --host 127.0.0.1 --port 4173`
  - `dashboard/playwright.config.ts` contains `?mock=1`
  - `npm --prefix dashboard run test:e2e` exits 0
</done>
<verify>
Run `npm --prefix dashboard run test:e2e`.
</verify>
</task>

</tasks>

<verification>
1. Run `npm --prefix dashboard run test:e2e -- tests/dashboard.spec.ts`
2. Run `npm --prefix dashboard run test:e2e -- tests/initial-layout.spec.ts`
3. Run `npm --prefix dashboard run test:e2e`
</verification>

<must_haves>
1. Quick Playwright coverage matches the final shell contract
2. Desktop layout coverage proves the route hero is gone
3. The full repo-local Playwright suite passes before phase verification
</must_haves>
