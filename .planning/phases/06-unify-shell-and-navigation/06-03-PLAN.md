---
phase: 06
plan: 03
title: Wire each route page into the shared mobile signal contract
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - dashboard/src/pages/Overview.tsx
  - dashboard/src/pages/Charts.tsx
  - dashboard/src/pages/Solar.tsx
  - dashboard/src/pages/RawData.tsx
  - dashboard/src/store/shell.ts
requirements_addressed:
  - UI-01
  - UI-02
must_haves:
  - Each page publishes the correct route-specific mobile signal
  - Signal reset behavior is explicit and measurable
  - Page-owned headers remain in the route body, not the shell
autonomous: true
---

<context>
After the thin shell exists, each route needs to provide its one approved mobile signal without moving its richer page framing back into the shell.
</context>

<objective>
Hook Overview, Charts, Solar, and Raw Data into the shared shell-signal contract and make stale-signal cleanup explicit.
</objective>

<tasks>

<task id="1">
<name>Publish Overview and Charts mobile shell signals</name>
<type>execute</type>
<files>
  - dashboard/src/pages/Overview.tsx
  - dashboard/src/pages/Charts.tsx
  - dashboard/src/store/shell.ts
</files>
<read_first>
  - dashboard/src/pages/Overview.tsx
  - dashboard/src/pages/Charts.tsx
  - dashboard/src/store/shell.ts
  - dashboard/src/lib/routes.ts
  - .planning/phases/06-unify-shell-and-navigation/06-UI-SPEC.md
</read_first>
<action>
Update the route pages so they publish dynamic mobile signal values through the shared shell store while the registry continues to own the static signal labels.

For `dashboard/src/pages/Overview.tsx`:
- publish the dynamic value for the registry-owned `Battery` signal by calling `setRouteSignal('overview', value)` with the same `total_battery_percent` reading already used in the overview hero
- format the value as `${battery}% reserve`
- add cleanup that calls `resetRouteSignal('overview')` on unmount

For `dashboard/src/pages/Charts.tsx`:
- publish the dynamic value for the registry-owned `Window` signal by calling `setRouteSignal('charts', value)` with the active `range.label` or selected range preset text
- add cleanup that calls `resetRouteSignal('charts')` on unmount

Keep the existing page-owned hero and explanatory copy inside each route body.
</action>
<done>
  - `dashboard/src/pages/Overview.tsx` contains `% reserve`
  - `dashboard/src/pages/Charts.tsx` contains `range.label`
  - both files call `setRouteSignal` with the correct route id
  - both files call `resetRouteSignal`
  - neither file removes its route-owned header or hero content
</done>
<verify>
Run `npm --prefix dashboard run build`.
</verify>
</task>

<task id="2">
<name>Publish Solar and Raw Data mobile shell signals with explicit reset behavior</name>
<type>execute</type>
<files>
  - dashboard/src/pages/Solar.tsx
  - dashboard/src/pages/RawData.tsx
  - dashboard/src/store/shell.ts
</files>
<read_first>
  - dashboard/src/pages/Solar.tsx
  - dashboard/src/pages/RawData.tsx
  - dashboard/src/store/shell.ts
  - dashboard/src/lib/routes.ts
  - .planning/phases/06-unify-shell-and-navigation/06-UI-SPEC.md
</read_first>
<action>
Update the remaining route pages so they publish the approved mobile shell signals and reset stale values when unmounted.

For `dashboard/src/pages/Solar.tsx`:
- publish the dynamic value for the registry-owned `Solar` signal by calling `setRouteSignal('solar', value)` with the existing live total-solar value
- format the value as `{N} W`

For `dashboard/src/pages/RawData.tsx`:
- publish the dynamic value for the registry-owned `Fields` signal by calling `setRouteSignal('raw', value)` with `${filtered.length} visible`

For both files:
- add cleanup that calls `resetRouteSignal(routeId)` when the page unmounts
- ensure the cleanup only resets the signal published by that route and does not remove page-owned content framing
</action>
<done>
  - `dashboard/src/pages/Solar.tsx` contains `W`
  - `dashboard/src/pages/RawData.tsx` contains `visible`
  - both files call `setRouteSignal` with the correct route id
  - both files call `resetRouteSignal`
  - the verify path can prove signal reset happens by leaving the route and observing a different route signal value
</done>
<verify>
Run `npm --prefix dashboard run build`.
</verify>
</task>

</tasks>

<verification>
1. Run `npm --prefix dashboard run build`
2. Read the four page files and confirm each one writes only the dynamic value while the route registry still owns the signal label
3. Confirm Solar and Raw Data include explicit `resetRouteSignal` cleanup
</verification>

<must_haves>
1. Each route publishes the approved one-signal mobile summary
2. No route hardcodes signal labels outside the route registry
3. Cleanup/reset behavior is explicit for the pages that publish shell signals
</must_haves>
