---
id: 260709-0fi
description: Refactor device files into data-driven definitions
status: active
tasks:
  - 1: Create enums.ts with all shared enum definitions
  - 2: Create definition.ts with DeviceDefinition type, BluettiDeviceModel, and all device data
  - 3: Refactor registry.ts to use definition data
  - 4: Remove individual device files
  - 5: Update index.ts exports
  - 6: Verify with tests
---

# Plan: Device Definition Refactor

## Goal

Replace the current 8 individual device class files (ac200m.ts, ac300.ts, ac500.ts, ac60.ts, eb3a.ts, ep500.ts, ep500p.ts, ep600.ts) with a single data-driven `definition.ts` that defines all devices as data objects consumed by a `BluettiDeviceModel` class.

## Tasks

### Task 1: Create `enums.ts`
Extract all shared enum definitions from `ac300.ts` and `eb3a.ts` into a dedicated `enums.ts` file.

**Files:** `src/devices/enums.ts` (new)
**Action:** Move enum constants + delete from source files
**Verify:** `enums.ts` exports all enums, source files import from `./enums.js`

### Task 2: Create `definition.ts`
Create `DeviceDefinition` interface and `BluettiDeviceModel` class. Define all 8 device configurations as data.

**Files:** `src/devices/definition.ts` (new)
**Action:** Write interface, class, and all device definitions
**Verify:** All 8 device types are representable, struct builders produce same fields

### Task 3: Refactor `registry.ts`
Replace switch/imports with definition lookup.

**Files:** `src/devices/registry.ts`
**Action:** Import definitions, use find() instead of switch
**Verify:** `createDeviceFromAdvertisement` produces same behavior

### Task 4: Remove individual device files
Delete `ac200m.ts`, `ac300.ts`, `ac500.ts`, `ac60.ts`, `eb3a.ts`, `ep500.ts`, `ep500p.ts`, `ep600.ts`.

### Task 5: Update `index.ts`
Replace individual exports with `definition.ts` and `enums.ts` exports.

### Task 6: Verify
Run existing tests to confirm nothing broke.
