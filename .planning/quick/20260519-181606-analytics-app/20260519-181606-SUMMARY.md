---
status: complete
---

# Quick Task 20260519-181606: Standalone Analytics App - Summary

## Completed

- Added a standalone `analytics/` package with React 19, TypeScript, Vite 8, TanStack Query, TanStack Router, uPlot, Tailwind 4, date-fns, and lucide-react.
- Implemented typed API helpers for live status, device/field discovery, bundled history, mock mode, and WebSocket message shapes.
- Implemented telemetry field metadata, AC500 field alias resolution, numeric field detection, time bucketing, summaries, peak detection, generated-energy delta calculation, and dense comparison timelines.
- Built the analytics page with device/range controls, KPI tiles, power balance chart, battery posture chart, resolved-field transparency, uPlot field comparison, and live snapshot cards.
- Fixed the standalone Vite proxy so `/api/*` requests are rewritten to the FastAPI root endpoints before forwarding.
- Removed Recharts from the analytics app after browser verification showed a React update-depth loop in the Recharts chart context; the analytics charts now use uPlot.
- Removed React `StrictMode` from the analytics dev entrypoint to avoid duplicate telemetry WebSocket/request churn during local development.
- Changed the local `bluetti-mqtt-node` console logger so warn-level BLE busy backoff events write through `console.warn` instead of `console.error`.

## Verification

- `npm install` completed in `analytics/` with 0 vulnerabilities.
- `npm run build` completed successfully in `analytics/`.
- Browser smoke test against `http://localhost:5120/` rendered the Analytics H1 and 6 KPI cards with no console errors or page errors.
- API proxy checks returned 200 for `/api/devices`, `/api/status`, and bundled `/api/history/...` requests.
- `npm run build` completed successfully in `lib/bluetti-mqtt-node`.
