---
status: completed
---

# Field comparison default settings summary

Added settings modal controls for selecting the default Field Comparison fields. The settings modal and Field Comparison panel now share the same comparable-field list, and selected defaults persist in local storage.

## Verification

- `npm run build` in `analytics/`
