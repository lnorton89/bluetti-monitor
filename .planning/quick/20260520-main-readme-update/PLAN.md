# Quick Task 20260520: Main README Update

## Goal

Update the root `README.md` so it reflects the standalone analytics app, bundled history endpoint, estimate backtest command, and current frontend stack.

## Plan

1. Compare the root README against the current app structure and recent commits.
2. Update stale README sections with analytics setup, API changes, command additions, and stack notes.
3. Commit the docs update if the README changes, then push the submodule and parent repo.
